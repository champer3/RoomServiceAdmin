# Copy to .env and set your backend URL. Do not commit .env.

# Backend API base URL (no trailing slash)
REACT_APP_API_URL=http://localhost:3000

# Optional: receipt printer IP
REACT_APP_PRINTER_IP=192.168.1.100
